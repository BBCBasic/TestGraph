name: TasteGraph v2 smoke

on:
  push:
    branches:
      - agent/tastegraph-v2-concepts
  pull_request:
    branches:
      - master

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      ENVIRONMENT: development
      DATABASE_URL: sqlite:///./test_v2.db
      APP_SECRET: test-secret-for-ci-only
      DEVELOPMENT_API_KEY: dev-secret
      PUBLIC_BASE_URL: http://localhost:8000
      ALLOWED_HOSTS: '["localhost","testserver"]'
      CORS_ORIGINS: '[]'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install -r requirements.txt
      - run: python -m compileall -q app
      - run: pytest -q tests/test_v2_concepts.py
      - run: rm -f test_v2.db && alembic upgrade head
